name: dzienny-bot
on:
  schedule:
    - cron: "10 0 * * *"      # codziennie 00:10 UTC (po zamknięciu dziennej świecy)
  workflow_dispatch: {}
permissions:
  contents: write
concurrency:
  group: bot
  cancel-in-progress: false
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Uruchom bota
        run: python3 bot.py
      - name: Zapisz wyniki
        run: |
          git config user.name "bot"
          git config user.email "bot@users.noreply.github.com"
          git add -A
          git diff --cached --quiet || (git commit -m "dane $(date -u +%F)" && git push)
